<Window x:Class="ActivityReader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:ActivityReader.Controls.Views;assembly=ActivityReader.Controls"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <MenuItem Header="File">
                <MenuItem Header="Download Data" Click="OpenDownloadWindow" />
            </MenuItem>
        </Menu>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button x:Name="LoadPreviousButton" Content="Previous" Click="LoadPreviousButton_Click" />
            <Button x:Name="LoadNextButton" Margin="10,0,0,0" Content="Next" Click="LoadNextButton_Click" />
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Vertical">
            <views:ActivityFeed x:Name="Feed" />
        </StackPanel>
    </Grid>
</Window>
